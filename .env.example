# PostgreSQL connection string used by Prisma.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/assetflow?schema=public"

# Auth.js
# Generate with: npx auth secret   (or: openssl rand -base64 32)
AUTH_SECRET="replace-with-a-random-32-byte-secret"

# Base URL of the deployed app (used by Auth.js for callbacks).
AUTH_URL="http://localhost:3000"

# Google OAuth (create credentials at https://console.cloud.google.com/apis/credentials)
AUTH_GOOGLE_ID="your-google-oauth-client-id"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# Resend — transactional email for OTP delivery during employee activation.
# Get a free API key at https://resend.com
RESEND_API_KEY="re_your_resend_api_key"

# Sender address shown on OTP emails.
#
# LOCAL DEV (Resend free plan, no verified domain):
#   Set RESEND_FROM to "onboarding@resend.dev"
#   IMPORTANT: Resend will only deliver to the email address that owns the
#   Resend account. The employee directory email must be that same address.
#   RESEND_FROM="onboarding@resend.dev"
#
# PRODUCTION (after verifying a domain at resend.com/domains):
#   RESEND_FROM="AssetFlow <no-reply@yourdomain.com>"
#   Emails will deliver to any recipient.
RESEND_FROM="onboarding@resend.dev"

# Demo Mode (Task 21, Part C) — passwords for the two seeded demo
# accounts (demo-admin@assetflow.dev, demo-employee@assetflow.dev).
# Optional: if unset, lib/demo.ts and prisma/seed.ts fall back to
# fixed demo-only defaults (DemoAdmin@2026 / DemoEmployee@2026).
# These accounts hold no real data — see src/lib/demo.ts for the
# server-side scope guards that keep demo mutations isolated.
DEMO_ADMIN_PASSWORD="DemoAdmin@2026"
DEMO_EMPLOYEE_PASSWORD="DemoEmployee@2026"
# Demo Super Admin (demo-super-admin@assetflow.dev). Stored as an ADMIN row;
# SUPER_ADMIN is granted only in the session. Default: DemoSuperAdmin@2026
DEMO_SUPER_ADMIN_PASSWORD="DemoSuperAdmin@2026"
